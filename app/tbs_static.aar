=====================================================
  这里要放 X5 静态内核文件，重命名为：
           tbs_static.aar
=====================================================

下载步骤：
1. 打开 https://x5.tencent.com/
2. 进入「开发文档 → 资源下载」
3. 找「自运营 / 静态内核 / 离线版」的下载链接
4. 下载得到 .tbs 文件，重命名为 tbs_static.aar
5. 放到这个目录下（app/libs/tbs_static.aar）

或者：
如果不要 X5，纯用系统 WebView：
→ 把 build.gradle.kts 里最后两行 X5 依赖注释掉即可
→ MainActivity.kt 不用改（已经兼容两种模式）
→ 用「系统浏览器登录」按钮过验证
